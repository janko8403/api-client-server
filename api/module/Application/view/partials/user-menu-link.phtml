<?php
    $label = $this->page->getLabel();
    if ($icon = $this->page->get('icon')) {
        $icon = $this->fontAwesome($icon);
        $label = $icon . ' ' . $label;
    }
?>
<?= sprintf(
    '<a href="%s" id="menu-%s" data-cookie="%s">%s</a>',
    $this->page->getHref(),
    $this->page->getId(),
    $this->page->get('cookie'),
    $label
) ?>